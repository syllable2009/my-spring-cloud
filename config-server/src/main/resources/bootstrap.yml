spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/syllable2009/my-spring-cloud.git
          search-paths: cloud-config-repo
#          username:
#          password:

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/rs/eureka/